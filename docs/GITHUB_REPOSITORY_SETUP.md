# GitHub repository creation settings

Create `Leoluis0705/energy-gated-da-tpp` as an **empty public repository**.

- Visibility: Public.
- Add README: Off. A maintained `README.md` is already present locally.
- Add `.gitignore`: None. A project-specific `.gitignore` is already present.
- Choose a license: No license at repository creation time.

The code and original data/figure licenses must be approved by the authors and
their institutions before public release. If approval is obtained, add the
approved license files to the local repository before the first public release.
Do not let GitHub generate an independent initial commit, because that would
create a history that must be reconciled with the validated local repository.

Suggested repository description:

> Reproducibility package for Energy-Gated DA-TPP, including active-learning
> code, frozen source data, figure builders, audit records, and final candidate
> structures.

